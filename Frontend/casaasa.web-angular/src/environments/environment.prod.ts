// This is the production environment configuration
// TODO: Replace the production and API URL values with the actual production values 
// before deploying the application to production.
export const environment = {
    production: true,
    apiUrl: "https://your-production-api-url.com/api"
}